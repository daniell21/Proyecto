<%= form_for(@accountpayable) do |f| %>
  <% if @accountpayable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accountpayable.errors.count, "error") %> prohibited this accountpayable from being saved:</h2>

      <ul>
      <% @accountpayable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
  <%= f.label :"Seleccione Proveedor" %><br>
  <%= f.collection_select :supplier_id, Supplier.all, :id, :name %>
</div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :total %><br>
    <%= f.number_field :total %>
  </div>
  
  <div class="actions">
    <%= f.submit 'Crear Cuenta'%>
  </div>
<% end %>
