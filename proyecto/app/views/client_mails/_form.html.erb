<%= simple_form_for(@client_mail) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  <%= f.input :date %>
  <%= f.association :client, :multiple => true %>

    <%= f.input :title %>
    <%= f.input :body %>
    

  </div>
<br><br>
<div align="right">
  <div class="form-actions">
    <%= f.button :submit %>
     <%= link_to 'Atrás', client_mails_path, :class => 'btn btn-default btn'  %>
  </div>

<% end %>
