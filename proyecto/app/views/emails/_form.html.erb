<%= simple_form_for([@client, @email]) do |f| %>
  <%= f.error_notification %>
<div class="row">
	<div class="col-xs-8">
	          <%= f.input :email, label: false, placeholder: 'Correo Electrónico' %>
	        </div>
	        <div class="col-xs-4">
	            <%= f.button :submit, "Añadir Correo" %>
	        </div>


</div>

  
<% end %>
