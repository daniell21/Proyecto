

<%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }, html: { class: 'form-inline' }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">

    <%= f.input :email, autofocus: true, placeholder: 'Correo' %>
  </div>
<br>
  <div class="actions">
    <%= f.button :submit, "Reenviar Instrucciones" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
